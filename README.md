# NgRx Sandbox

Project for experiments with NgRx technology.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.


## JSON server

Run `json-server src/db.json` to enable reading and writing to database.